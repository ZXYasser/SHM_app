# 📤 دليل رفع المشروع على GitHub

هذا الدليل يشرح كيفية رفع مشروعك على GitHub خطوة بخطوة.

---

## ✅ الخطوة 1: التحضير

تم إنشاء الملفات التالية:
- ✅ `.gitignore` - لحماية الملفات الحساسة
- ✅ `README.md` - وصف المشروع

**مهم جداً**: تأكد من أن `firebase-key.json` في `.gitignore` (تم إضافته بالفعل)

---

## 🔧 الخطوة 2: تهيئة Git

افتح Terminal في مجلد المشروع (`D:\pR\SHM_app`) وقم بتنفيذ:

```bash
# تهيئة Git repository
git init

# إضافة جميع الملفات
git add .

# Commit أولي
git commit -m "Initial commit: SHM - ورشة سيارات متنقلة"
```

---

## 🌐 الخطوة 3: إنشاء Repository على GitHub

1. اذهب إلى [github.com](https://github.com)
2. اضغط على **"+"** في الأعلى → **"New repository"**
3. املأ التفاصيل:
   - **Repository name**: `SHM_app` (أو أي اسم تريده)
   - **Description**: "نظام إدارة ورشة سيارات متنقلة"
   - **Visibility**: 
     - ✅ **Public** (إذا أردت مشاركته)
     - ✅ **Private** (إذا أردت إبقاءه خاص)
   - **لا** تضع علامة على "Initialize with README" (لأن لدينا README بالفعل)
4. اضغط **"Create repository"**

---

## 📤 الخطوة 4: ربط المشروع المحلي بـ GitHub

بعد إنشاء Repository، GitHub سيعطيك أوامر. استخدم هذه الأوامر:

```bash
# إضافة remote repository
git remote add origin https://github.com/YOUR_USERNAME/SHM_app.git

# تغيير اسم الفرع الرئيسي إلى main (إذا لزم الأمر)
git branch -M main

# رفع المشروع
git push -u origin main
```

**ملاحظة**: استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك على GitHub

---

## 🔐 الخطوة 5: المصادقة (Authentication)

إذا طُلب منك اسم المستخدم وكلمة المرور:

### الطريقة 1: Personal Access Token (موصى به)
1. اذهب إلى GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. اضغط "Generate new token"
3. اختر الصلاحيات: `repo` (كامل)
4. انسخ الـ Token
5. استخدمه كـ password عند `git push`

### الطريقة 2: GitHub CLI
```bash
# تثبيت GitHub CLI
winget install GitHub.cli

# تسجيل الدخول
gh auth login

# ثم حاول git push مرة أخرى
```

---

## ✅ الخطوة 6: التحقق

بعد الرفع، اذهب إلى repository على GitHub وتأكد من:
- ✅ جميع الملفات موجودة
- ✅ `firebase-key.json` **غير موجود** (محمي في .gitignore)
- ✅ `README.md` يظهر بشكل صحيح

---

## 🔄 تحديثات لاحقة

عندما تقوم بتعديلات في المستقبل:

```bash
# إضافة التغييرات
git add .

# Commit
git commit -m "وصف التغييرات"

# رفع التحديثات
git push
```

---

## 📋 ملخص الأوامر الكاملة

```bash
# 1. تهيئة Git
git init

# 2. إضافة الملفات
git add .

# 3. Commit أولي
git commit -m "Initial commit: SHM - ورشة سيارات متنقلة"

# 4. ربط بـ GitHub (استبدل YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/SHM_app.git

# 5. تغيير اسم الفرع
git branch -M main

# 6. رفع المشروع
git push -u origin main
```

---

## ⚠️ ملاحظات مهمة

### 1. حماية الملفات الحساسة
- ✅ `firebase-key.json` موجود في `.gitignore`
- ✅ لن يُرفع إلى GitHub
- ✅ استخدم Environment Variables في Production

### 2. حجم المشروع
- Flutter projects قد تكون كبيرة
- إذا كان حجم المشروع كبير جداً، يمكنك:
  - رفع `SHM_backend` فقط أولاً
  - أو استخدام Git LFS للملفات الكبيرة

### 3. إذا واجهت مشاكل
- **"remote origin already exists"**: 
  ```bash
  git remote remove origin
  git remote add origin https://github.com/YOUR_USERNAME/SHM_app.git
  ```

- **"failed to push"**: 
  - تأكد من المصادقة (Personal Access Token)
  - تأكد من أن Repository موجود على GitHub

---

## 🎉 النتيجة

بعد اكتمال الخطوات:
- ✅ المشروع على GitHub
- ✅ يمكنك مشاركته مع الآخرين
- ✅ يمكنك استخدامه مع Railway للنشر
- ✅ يمكنك العمل من أي مكان

**الآن يمكنك نشر Backend على Railway من GitHub! 🚀**

