# 🧪 دليل اختبار المشروع بعد النشر على Railway

بعد نشر Backend على Railway، اتبع هذا الدليل لاختبار جميع المكونات.

---

## ✅ الخطوة 1: التحقق من Backend

### 1.1 الحصول على URL
1. اذهب إلى Railway Dashboard
2. افتح مشروعك
3. في **Settings** → **Networking** → **Generate Domain**
4. ستحصل على URL مثل: `https://your-app.up.railway.app`

### 1.2 اختبار Health Check
افتح المتصفح واذهب إلى:
```
https://your-app.up.railway.app/health
```

**يجب أن ترى**:
```json
{
  "server": "running",
  "firebase": "connected",
  "timestamp": "..."
}
```

✅ إذا رأيت هذا، Backend يعمل بشكل صحيح!

---

## 🔧 الخطوة 2: تحديث URLs في التطبيقات

### 2.1 تحديث Flutter Customer App

افتح `flutter_app/lib/utils/constants.dart`:

```dart
static String get baseUrl {
  if (kIsWeb) {
    return 'https://your-app.up.railway.app';  // ← غيّر هنا
  } else {
    return 'https://your-app.up.railway.app';  // ← غيّر هنا
  }
}
```

**استبدل** `your-app.up.railway.app` بـ URL الفعلي من Railway.

### 2.2 تحديث Flutter Technician App

افتح `SHM_technician_app/lib/utils/constants.dart`:

```dart
static String get baseUrl {
  if (kIsWeb) {
    return 'https://your-app.up.railway.app';  // ← غيّر هنا
  } else {
    return 'https://your-app.up.railway.app';  // ← غيّر هنا
  }
}
```

### 2.3 تحديث React Dashboard

افتح جميع ملفات Dashboard وابحث عن `http://localhost:3000`:

**الملفات التي تحتاج تحديث**:
- `SHM_dashboard/src/Orders.jsx`
- `SHM_dashboard/src/OrderDetails.jsx`
- `SHM_dashboard/src/Technicians.jsx`
- `SHM_dashboard/src/Login.jsx` (إذا كان يستخدم API)

**استبدل**:
```javascript
// القديم
const res = await fetch('http://localhost:3000/requests', {
  // ...
});

// الجديد
const res = await fetch('https://your-app.up.railway.app/requests', {
  // ...
});
```

**أو** أنشئ ملف `.env` في `SHM_dashboard/`:
```env
VITE_API_URL=https://your-app.up.railway.app
```

ثم استخدمه:
```javascript
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000';
const res = await fetch(`${API_URL}/requests`, {
  // ...
});
```

---

## 🧪 الخطوة 3: اختبار كل مكون

### 3.1 اختبار Flutter Customer App

#### على Web:
```bash
cd flutter_app
flutter run -d chrome
```

**ما يجب اختباره**:
1. ✅ فتح التطبيق
2. ✅ اختيار نوع الخدمة
3. ✅ إدخال بيانات السيارة
4. ✅ تحديد الموقع (قد يطلب إذن)
5. ✅ إرسال الطلب
6. ✅ يجب أن ترى رسالة نجاح

**إذا فشل**:
- تحقق من Console (F12) للأخطاء
- تأكد من أن URL صحيح في `constants.dart`
- تأكد من أن Backend يعمل (اختبر `/health`)

#### على Mobile (Android/iOS):
```bash
flutter run
```

**نفس الاختبارات** + تأكد من:
- ✅ الاتصال بالإنترنت
- ✅ GPS مفعّل
- ✅ URL صحيح في `constants.dart`

---

### 3.2 اختبار Flutter Technician App

#### تسجيل الدخول:
1. ✅ افتح التطبيق
2. ✅ أدخل رقم الجوال وكلمة المرور
3. ✅ يجب أن ينتقل للصفحة الرئيسية

**إذا فشل**:
- تحقق من أن الفني موجود في Firebase
- تحقق من Console للأخطاء
- تأكد من URL صحيح

#### عرض الطلبات:
1. ✅ يجب أن ترى الطلبات المخصصة له + الطلبات الجديدة
2. ✅ يمكنك فلترة حسب الحالة
3. ✅ يمكنك فتح تفاصيل الطلب

#### تحديث حالة الطلب:
1. ✅ افتح تفاصيل طلب
2. ✅ غيّر الحالة (قيد التنفيذ → مكتمل)
3. ✅ يجب أن يتحدث في Dashboard أيضاً

---

### 3.3 اختبار React Dashboard

#### تسجيل الدخول:
```bash
cd SHM_dashboard
npm run dev
```

1. ✅ افتح `http://localhost:5174`
2. ✅ سجل دخول (username/password)
3. ✅ يجب أن ترى صفحة الطلبات

#### عرض الطلبات:
1. ✅ يجب أن ترى جميع الطلبات
2. ✅ الإحصائيات تظهر بشكل صحيح
3. ✅ يمكنك فلترة حسب الحالة

#### تعيين فني:
1. ✅ افتح تفاصيل طلب
2. ✅ اختر فني من القائمة
3. ✅ اضغط "تعيين الفني للطلب"
4. ✅ يجب أن يظهر اسم الفني في الجدول

#### إدارة الفنيين:
1. ✅ اذهب لصفحة الفنيين
2. ✅ أضف فني جديد
3. ✅ يجب أن يظهر في القائمة
4. ✅ يمكنك حذف فني

---

## 🔍 الخطوة 4: اختبار التكامل الكامل

### سيناريو كامل:

1. **العميل يرسل طلب**:
   - افتح Flutter Customer App
   - أرسل طلب جديد
   - ✅ يجب أن يظهر في Dashboard

2. **المدير يعين فني**:
   - افتح Dashboard
   - افتح تفاصيل الطلب الجديد
   - عيّن فني للطلب
   - ✅ يجب أن يظهر في تطبيق الفني

3. **الفني يكمل الطلب**:
   - افتح Flutter Technician App
   - سجل دخول
   - افتح الطلب المخصص له
   - غيّر الحالة إلى "مكتمل"
   - ✅ يجب أن يتحدث في Dashboard

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "Failed to fetch" أو "Connection timeout"

**الحل**:
1. تحقق من أن Backend يعمل: `https://your-app.up.railway.app/health`
2. تحقق من أن URL صحيح في `constants.dart`
3. تحقق من Logs في Railway Dashboard

### المشكلة: "CORS error"

**الحل**:
- Backend يجب أن يكون مضبوط على `origin: '*'` (موجود بالفعل)
- إذا استمرت المشكلة، تحقق من Railway Logs

### المشكلة: "Firebase initialization failed"

**الحل**:
1. تحقق من Environment Variables في Railway
2. تأكد من أن جميع المتغيرات موجودة
3. تحقق من Logs في Railway

### المشكلة: Dashboard لا يفتح

**الحل**:
1. تأكد من تحديث جميع URLs من `localhost:3000` إلى Railway URL
2. تحقق من Console (F12) للأخطاء
3. تأكد من أن Dashboard يتصل بـ Railway URL

---

## ✅ Checklist النهائي

قبل اعتبار المشروع جاهز:

- [ ] Backend يعمل على Railway (`/health` يعمل)
- [ ] Flutter Customer App يرسل طلبات بنجاح
- [ ] Flutter Technician App يسجل دخول ويعرض الطلبات
- [ ] Dashboard يعرض الطلبات والفنيين
- [ ] المدير يمكنه تعيين فني للطلب
- [ ] الفني يمكنه تحديث حالة الطلب
- [ ] جميع التحديثات تظهر في الوقت الفعلي

---

## 🎉 النتيجة

إذا نجحت جميع الاختبارات:
- ✅ المشروع يعمل بشكل كامل
- ✅ Backend يعمل 24/7 على Railway
- ✅ لا حاجة لتشغيل الابتوب
- ✅ يمكن اختبار التطبيق من أي مكان!

**مبروك! المشروع جاهز للاستخدام! 🚀**

